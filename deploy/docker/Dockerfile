# Legacy single-image Dockerfile removed.
# Build api and web images from their component Dockerfiles:
#   docker build -t carlosrabelo/tatuscan-api:latest ../../api
#   docker build -t carlosrabelo/tatuscan-web:latest ../../web
# Or: make stack-build
