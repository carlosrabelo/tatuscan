# Secret for TatuScan sensitive data
apiVersion: v1
kind: Secret
metadata:
  name: tatuscan-secrets
  namespace: tatuscan
type: Opaque
stringData:
  # Bearer token for POST/PATCH/DELETE. Empty = open writes (LAN only).
  TATUSCAN_API_TOKEN: ""
