# Porta exposta pelo app (interno ao container/app)
TATUSCAN_PORT=8040

# Opcional: porta alternativa para ambientes fora do container
# PORT=8040

# Diretório e arquivo do banco SQLite (quando não usar SQLALCHEMY_DATABASE_URI)
# No container, /data é persistido via volume
TATUSCAN_DB_DIR=/data
TATUSCAN_DB_FILE=tatuscan.db

# URI do banco (se definido, ignora TATUSCAN_DB_DIR/FILE)
# Exemplos:
# SQLALCHEMY_DATABASE_URI=sqlite:////data/tatuscan.db
# SQLALCHEMY_DATABASE_URI=postgresql+psycopg2://user:pass@host:5432/dbname
# SQLALCHEMY_DATABASE_URI=mysql+pymysql://user:pass@host:3306/dbname

# Fuso horário usado para exibição
TIMEZONE=America/Cuiaba

# Chave secreta do Flask
SECRET_KEY=change-me

# Opcional para docker compose: porta do host
# HOST_PORT=8040
